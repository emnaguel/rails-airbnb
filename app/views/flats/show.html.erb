
<h1><%= @flat.name %></h1>
<%= @flat.address %>
<%= @flat.description %>
<%= @flat.price_per_night %>
<%= @flat.number_of_guests %>

<p><%= link_to "Back", flats_path %></p>

<%= link_to "Remove", @flat_path, method: :delete, data: { confirm: "Are you sure?"} %></p>
